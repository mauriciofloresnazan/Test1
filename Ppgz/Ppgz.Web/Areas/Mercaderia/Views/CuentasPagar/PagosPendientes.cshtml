@using Microsoft.AspNet.Identity
@{
    ViewBag.Title = "Gestión de Pagos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="form-signin-heading">Gestión de Pagos Pendientes</h2>
<p class="page-description-title"><i class="fa fa-calculator" aria-hidden="true"></i> <i>En esta sección podrá gestionar los pagos que tiene con el Grupo Nazan.</i><strong>@HttpContext.Current.Session["razonsocial"] </strong>   <a href="@Url.Action("Index", "CuentasPagar")">Cambiar</a></p>

<div class="panel panel-default">
    <div class="panel-heading">Pagos Pendientes</div>
    <div class="panel-body">
        <div class="col-md-12">
            <table id="pagosPendiente" class="table table-striped table-condensed table-bordered">
                <thead>
                    <tr>
                        <th class="text-center">Fecha</th>
                        <th class="text-center">Proveedor RFC</th>
                        <th class="text-center">Proveedor Razón Social</th>
                        <th class="text-center">Importe</th>
                        <th class="text-center">Detalle</th>
                    </tr>
                </thead>
                <tbody>

                    <tr>
                        <td class="text-center">@DateTime.Today.ToString("dd/MM/yyyy")</td>
                        <td class="text-center">@ViewBag.proveedor.Rfc</td>
                        <td class="text-center">@ViewBag.proveedor.Nombre1</td>
                        <td class="text-center">@ViewBag.importeTotal</td>
                        <td class="text-center">
                            <a class="btn btn-success btn-xs" href="@Url.Action("PagosPendientesDetalle", "CuentasPagar", new { proveedorId = ViewBag.proveedor.Id })">Detalle</a>
                        </td>
                    </tr>

                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <script type="text/javascript">
        $(document).ready(function () {
            $('#pagosPendiente').DataTable({
                "language": {
                    "url": "https://cdn.datatables.net/plug-ins/1.10.13/i18n/Spanish.json"
                },
                "lengthMenu": [[5, 10, 25, 50, -1], [5, 10, 25, 50, "Todos"]]
            });
        });
    </script>
}



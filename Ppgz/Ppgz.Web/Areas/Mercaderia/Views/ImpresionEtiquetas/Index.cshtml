@using Ppgz.Repository
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Gestión de Impresión de Etiquetas</h2>
<p class="page-description-title"><i class="fa fa-address-book" aria-hidden="true"></i> <i>En esta sección se podrán Imprimir las etiquetas asociadas en su cuenta.</i></p>

<div class="panel panel-default">
    <div class="panel-heading">Impresión de Etiquetas</div>
    <div class="panel-body">
        <div class="col-md-12">
            <table id="etiquetas" class="table table-striped table-condensed table-bordered">
                <thead>
                    <tr>
                        <th class="text-center">Orden</th>
                        <th class="text-center">Fecha Recepción</th>
                        <th class="text-center">RFC</th>
                        <th class="text-center">Razón Social</th>
                        <th class="text-center">Descargar</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (etiqueta etiqueta in ViewBag.Etiquetas)
                    {
                        <tr>
                            <td class="text-center">@etiqueta.NumeroOrden</td>
                            <td class="text-center">@etiqueta.Fecha.ToString("dd/MM/yyyy")</td>
                            <td class="text-center">@etiqueta.proveedore.Rfc</td>
                            <td class="text-center">@etiqueta.proveedore.Nombre1 @etiqueta.proveedore.Nombre2 @etiqueta.proveedore.Nombre3 @etiqueta.proveedore.Nombre4</td>
        
                            <td class="text-center vert-align">
                                <a href="@Url.Action("Descargar", "ImpresionEtiquetas", new {@id = @etiqueta.Id})">
                                    <i class="fa fa-file-excel-o" aria-hidden="true"></i> Descargar
                                </a>
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
    </div>
</div>
@section Scripts {
    <script type="text/javascript">

        $(document).ready(function () {

            $('#etiquetas').DataTable({
                "language": {
                    "url": "@Url.Content("~/assets/vendor/dataTables-1.10.13/Spanish.json")"
                },
                "lengthMenu": [[10, 25, 50, -1], [10, 25, 50, "Todos"]]
            });

        });

    </script>
}


